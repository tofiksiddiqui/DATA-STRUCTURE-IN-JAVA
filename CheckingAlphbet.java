public class CheckingAlphbet {
    public static void main(String[] args) {
        char ch = 'W';

        if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'i' || ch == 'o' || ch == 'u') {
            System.out.println(ch + " is an alphabet character ");
        } else {
            System.out.println("It is not alphabet but it is consonent");
        }
    }
}
